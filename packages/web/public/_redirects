# SPA routing: any non-static path serves index.html so client-side routes
# (/methodology, /privacy) render. Missing /assets/* paths are NOT covered here —
# a real 404 is returned by functions/assets/[[path]].js (deploy-race protection),
# scoped to /assets/* only via _routes.json. See test/deploy-hygiene.test.ts.
/*           /index.html    200
