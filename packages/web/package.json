{
  "name": "@oresund/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/generate-icons.ts && vite build && tsx scripts/prerender.ts && tsx scripts/generate-llms.ts",
    "dev": "vite",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "typescript": "^5.5.0",
    "vite": "^5.0.0"
  }
}
